%h1 Time Records By User
%table
  %thead
    %tr
      %th Comment
      %th Time type
      %th Started time
      %th Finished time
      %th Time Duration
      %th{:colspan => "3"}
  %tbody
    - @time_records.each do |time_record|
      %tr
        %td= time_record.comment
        %td= time_record.time_type
        %td= time_record.started_time.strftime("%H:%M:%S | %d/%m/%Y") if time_record.started_time != nil
        %td= time_record.finished_time.strftime("%H:%M:%S | %d/%m/%Y") if time_record.finished_time != nil
        %td= time_duration(time_record)
        %td= link_to 'Show', time_record
        %td= link_to 'Edit', edit_time_record_path(time_record)
        %td= link_to 'Destroy', time_record, method: :delete, data: { confirm: 'Are you sure?' }