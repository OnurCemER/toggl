<%= form_with(model: time_record, local: true) do |form| %>
  <% if time_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(time_record.errors.count, "error") %> prohibited this time_record from being saved:</h2>

      <ul>
        <% time_record.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_field :comment %>
  </div>

  <div class="field">
    <%= form.label :time_type %>
    <%= form.text_field :time_type %>
  </div>

  <div class="field">
    <%= form.label :started_time %>
    <%= form.datetime_select :started_time %>
  </div>

  <div class="field">
    <%= form.label :finished_time %>
    <%= form.datetime_select :finished_time %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
